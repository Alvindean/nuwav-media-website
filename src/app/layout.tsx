import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'NU WAV Media - Premium Marketing for Luxury Industries',
  description: 'Specialized marketing for real estate, legal, healthcare, and luxury brands. Drive growth with data-driven strategies.',
  keywords: 'luxury marketing, real estate marketing, legal marketing, healthcare marketing, premium digital marketing',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
